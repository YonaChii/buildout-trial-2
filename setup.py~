from setuptools import setup, find_packages
 
setup(
  name='test',
  version= "1.0",
  description='trying 2nd tutorial',
  author='Yona',
  packages=['exam',], #find_packages('src/exam'),
  package_dir={ '' : 'src/exam' },
  install_requires = ['django == 1.5.5'],
)
